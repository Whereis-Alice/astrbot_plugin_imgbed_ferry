# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.1.0] - 2026-09-16

### 新增

- **跨插件资源上传 API**：新增 `upload_asset(event, asset, folder, compress, output_format)`，可接收 meme_magpie 的短期资源句柄并返回公网 URL、Markdown 和复用状态，供 AstrBook 等插件调用。
- **受控句柄校验**：校验来源、协议、TTL、大小、SHA-256、图片扩展名、文件签名与 MIME；拒绝裸路径、符号链接和失效句柄，且不会删除提供方源文件。
- **上下文感知去重**：最终上传内容按图床配置、目录、命名方式和压缩策略隔离；同一指纹的跨请求并发上传只发一个请求，并使用稳定 `Idempotency-Key` 重试。
- **错误码与联动文档**：跨插件调用失败时返回稳定 `code`，补充 AstrBook / meme_magpie 对接示例。
- **配额语义优化**：跨插件上传只在确认没有可复用缓存、即将发起新请求时预留额度；重复发帖命中缓存不再被错误拦截。

### 兼容性

- 原有 LLM 工具、命令、批量上传、压缩包解压和 OneBot 取件行为保持不变。
- 同一批次中内容、目录和上传选项都相同的文件继续复用链接；压缩包中不同目录的文件按不同目录分别上传。

## [v1.0.0] - 2026-09-05

首个版本。

### 新增

- **自然语言上传**：`imgbed_upload` 等 5 个 LLM 工具，直接说「把这张图传图床」「引用那个压缩包解压后全传上去」即可。
- **批量上传**：一条消息里的多图多文件、引用消息里的附件、会话里缓存的历史素材都能一次送走。
- **压缩包解压上传**：zip / 7z / rar / tar 系列解压后逐个上传，带 zip slip、解压炸弹、乱码文件名与密码处理。
- **外链转存**（默认关闭）：把用户给出的 http/https 直链转存进图床，内置 SSRF 防护与域名白名单。
- **sha256 去重**：跨批次 KV 缓存 + 批内复用，重复文件直接返回旧链接，不占图床空间。
- **图片预压缩**：超过阈值的图片先转 WebP / 缩边再上传，可按次覆盖。
- **并发与重试**：可配并发度、按错误类型分类重试，单文件 / 单次总量 / 文件数三重上限。
- **目录模板**：`{date}` `{group}` `{sender}` `{platform}` `{type}` `{ext}` 等变量。
- **权限与配额**：群黑白名单、用户黑名单、仅管理员模式、每人 / 每群每日上传配额。
- **回执样式**：plain / markdown / html / bbcode 四种链接风格，数量多时自动折叠成合并转发。
- **指令**：`/图床` `/图床上传` `/图床列表` `/图床记录` `/图床配额`，以及管理员的 `/图床自检` `/图床文件` `/图床删除` `/图床清理`。
- **OneBot 适配**：对 LLOneBot / NapCat / SnowLuma 做了文件直链获取适配，并绕过官方适配器 `event.message[0]` 硬编码索引导致的文件段丢失问题（含引用消息）。

### 已知限制

- `/api/manage/quota` 需要 manage 权限，只配 API Token 时必然 401。
- 图床的分块上传接口暂未接入，超大文件请走图床网页端。
